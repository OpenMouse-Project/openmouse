{
  "name": "openmouse",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "test": "node --test \"src/**/*.test.ts\"",
    "size": "node build/check-bundle-size.ts",
    "check": "npm run build && npm test",
    "preview": "vite preview"
  },
  "dependencies": {
    "@openmouse/protocol": "https://github.com/jazzstack/mouse-protocol.git#b07567c72c3aa04ac71c94ad31814de1fbe9fa8c",
    "preact": "^10.29.8"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vite": "^6.3.5"
  }
}
